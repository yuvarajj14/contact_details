<%= form_for @contact_detail do |f| %>
 
  <% if @contact_detail.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@contact_detail.errors.count, "error") %> prohibited
        this contact detail from being saved:
      </h2>
      <ul>
        <% @contact_detail.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
 
  <p>
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </p>
  
  <!--<%= f.fields_for :addresses do |builder| %>
    <%= render "address_fields", :f => builder %>
  <% end %>

  <%= f.fields_for :phone_numbers do |builder| %>
    <%= render "phone_number_fields", :f => builder %>
  <% end %>

  <p><%= link_to_add_fields "Add Another Address", f, :addresses %></p>

  <p><%= link_to_add_fields "Add Another Phone Number", f, :phone_numbers %></p>-->

  
 






  <ul class="tab">
    <li><a href="javascript:void(0)" class="tablinks" onclick="openCity(event, 'Address')">Address</a></li>
    <li><a href="javascript:void(0)" class="tablinks" onclick="openCity(event, 'Phone_number')">Phone number</a></li>
  </ul>

  <div id="Address" class="tabcontent">
    <%= f.fields_for :addresses do |builder| %>
      <%= render "address_fields", :f => builder %>
    <% end %>
    <p><%= link_to_add_fields "Add Another Address", f, :addresses %></p>
  </div>

  <div id="Phone_number" class="tabcontent">
    <%= f.fields_for :phone_numbers do |builder| %>
      <%= render "phone_number_fields", :f => builder %>
    <% end %>
    <p><%= link_to_add_fields "Add Another Phone Number", f, :phone_numbers %></p>
  </div>

  <p>
    <%= f.submit %>
  </p>

<% end %>